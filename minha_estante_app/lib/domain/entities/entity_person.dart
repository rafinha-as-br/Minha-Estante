/// this entity represents a random person that uses the app, this entity is
/// used to represent the public person data
class Person {

  String name;
  String institution;
  String userType;

  Person(this.name, this.institution, this.userType);
}